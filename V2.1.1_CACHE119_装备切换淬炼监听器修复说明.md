# V2.1.1 CACHE119 · 装备切换淬炼监听器修复

## 现象
第一件装备可以正常使用兵魄/护道/百炼；关闭后打开第二件装备，按钮可能无反应或提示旧装备状态；退出游戏重新进入后暂时恢复。

## 根因
`b-equipment-v210.js` 旧版把弹窗 click/change 监听器绑定到永久存在的 `#modalRoot`。关闭弹窗只清空 `innerHTML`，不会移除 `modalRoot` 上的监听器。每打开一件装备都会累积一个新监听器，并在闭包里保留当时的旧装备对象。

第二件装备点击时，最早的旧监听器先触发，调用旧装备ID并将 `state.busy=true`；当前装备监听器随后因 busy 直接退出。重进游戏会销毁页面监听器，所以暂时恢复。

## 修复
- 监听器改绑到本次新建的 `.forge-backdrop-v210` 节点。
- 关闭弹窗时该节点被删除，监听器自动销毁，不再累积。
- 兵魄/护道/百炼/升品/破境/器魂承接点击时实时读取 `state.item`。
- 仍保留 SQL247 的实时 `location/is_locked` 复核。
- 无新增SQL；SQL247 R2继续作为生产数据库基线，下一编号SQL248。
