# 九霄问道 V1.0 FIX1 CACHE31 完整部署包

## 已部署 V1.0 CACHE30
1. Supabase执行 `32_V1.0_FIX1_升级前检查.sql`。
2. 执行 `33_V1.0_FIX1_挑战战报参数兼容修复.sql`。
3. 完整覆盖GitHub仓库并推送 `main`。
4. Pages发布成功后执行 `34_V1.0_FIX1_CACHE31_正式发布门禁.sql`。
5. 执行 `35_V1.0_FIX1_升级后检查.sql`。

## 从 V0.15.4 FIX5 CACHE25 全新升级
依次执行 `26`、`27`、`33`，再部署完整前端；发布成功后执行 `34`、`35`。无需执行旧的28号CACHE30门禁。

## 本次修复
- 灵根名称追加当前本命五行；火字为红色。
- 手机端元神中央区域压缩约10%。
- 洞府储物改为6×6、每层36格，并保留名称和类型小字。
- 修复挑战结算发布九霄界闻时integer/smallint不匹配导致的整场回滚。
