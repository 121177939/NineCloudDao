# 九霄问道 V1.1 CACHE35 完整部署包

这是当前唯一权威完整部署包。解压后把全部文件覆盖到 GitHub 仓库根目录，不要只挑选前端文件。

## 从 V1.0 FIX4 CACHE34 升级

1. Supabase 执行49号升级前检查。
2. 执行50号双向战力挑战与界闻修复。
3. 覆盖并部署本完整前端包，等待 GitHub Pages 成功。
4. 执行51号 CACHE35 发布门禁。
5. 执行52号升级后检查，所有 `ok` 必须为 `true`。

异常时可执行53号只关闭战力挑战；确认修复后执行54号恢复。

不要重新执行27号覆盖新挑战规则；不要把40号FIX3脚本当作本次修复，50号才是正确增量迁移。
