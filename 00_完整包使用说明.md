# 九霄问道 V1.1 FIX1 CACHE36 完整部署包

这是当前唯一权威完整部署包。解压后将全部文件覆盖到 GitHub 仓库根目录，不要只挑选前端文件。

## 从 V1.1 CACHE35 升级

1. 暂停赌坊前端新流量，等待当前鱼虾公共轮次自然结算。
2. Supabase 执行 `58_V1.1_FIX1_升级前检查.sql`。
3. 执行 `59_V1.1_FIX1_赌场周期资金与公平结算.sql`。
4. 执行 `60_V1.1_FIX1_CACHE36_正式发布门禁.sql`。
5. 执行 `61_V1.1_FIX1_升级后检查.sql`，所有 `ok` 必须为 `true`。
6. 覆盖并部署本完整前端包，等待 GitHub Pages 成功并确认客户端显示 `V1.1 FIX1 CACHE36`。

紧急情况下执行62号关闭赌坊；确认61号全部通过后，执行63号恢复。

本次只追加58—63号SQL，不得修改或重新编号历史正式迁移。后续正式SQL从64号继续。
