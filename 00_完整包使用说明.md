# 九霄问道 V0.15.4 CACHE23 完整部署包

1. 将本包全部文件覆盖到GitHub仓库根目录并提交。
2. GitHub Actions会运行 `python3 tools/ci_v0154.py .`，验证后构建Pages制品。
3. 数据库按 `SQL/00_SQL执行说明.txt` 顺序执行V0.15.4脚本。
4. CACHE23会强制旧客户端清理PWA缓存并载入新版本。

- SQL FIX1：修复珍宝阁丹药写入 item_definitions 时类别约束冲突，并显式启用新表 RLS。
