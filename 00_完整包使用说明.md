# 九霄问道 V1.4 CACHE42 完整包

将本包全部内容覆盖到GitHub仓库根目录，包括隐藏目录`.github`。

生产库已完成V1.3 CACHE41时，依次执行：

1. `85_V1.4_九霄牌九自动准备与私密明牌.sql`
2. `86_V1.4_CACHE42_正式发布门禁.sql`
3. `87_V1.4_CACHE42_升级后检查.sql`

87号所有`ok`必须为`true`后再推送完整前端包到`main`。

V1.4不会修改牌型、赔率、2.5%手续费、赌场资金规则、变异灵根或五行战斗体系。
