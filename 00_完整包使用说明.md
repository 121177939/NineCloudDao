# 完整包使用说明

本目录是 **V1.2 FIX2 CACHE39** 可直接覆盖 GitHub 仓库根目录的完整包。

## 已经完成V1.2 FIX1 CACHE38且77、78均已执行

1. 执行 `79_V1.2_FIX2_CACHE39_牌九预览界面发布门禁.sql`。
2. 执行 `80_V1.2_FIX2_CACHE39_升级后检查.sql`，所有 `ok` 必须为 `true`。
3. 用完整部署包覆盖仓库根目录并发布。

## 已经完成72号，但尚未执行77、78

按 `77 → 78 → 79 → 80` 执行，再覆盖发布完整部署包。

## 从V1.2 CACHE37全新升级

按 `71 → 72 → 73 → 74 → 79 → 80` 执行。包内72号已经合并原语法修复以及77、78修复，不需要重复执行77、78。

75、76仅用于九霄牌九紧急停用与恢复，正常升级不要执行。
