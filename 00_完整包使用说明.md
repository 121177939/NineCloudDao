# 九霄问道 V1.0 FIX4 CACHE34 完整部署包

## 从 V1.0 FIX3 CACHE33 升级

1. Supabase执行 `44_V1.0_FIX4_升级前检查.sql`。
2. Supabase执行 `45_V1.0_FIX4_赌坊安全结算.sql`。
3. 解压本完整包并全部覆盖GitHub仓库根目录，推送到 `main`。
4. 等待GitHub Pages工作流 `python3 tools/ci_v1_fix4.py .` 全部通过并发布完成。
5. 执行 `46_V1.0_FIX4_CACHE34_正式发布门禁.sql`。
6. 执行 `47_V1.0_FIX4_升级后检查.sql`。

45号SQL执行后旧赌坊RPC会立即撤权，因此建议连续完成第2至第4步，缩短维护窗口。

本版核心规则：单局最多当前可用资源10%；玩家庄无系统兜底、必须覆盖最大赔付；玩家庄有效局5%进入全服造化池；重复request_id不会重复结算。
