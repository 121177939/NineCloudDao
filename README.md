# 九霄问道 Web Alpha V0.15.3 FIX2 CACHE22

当前部署版本：**V0.15.3 FIX2 CACHE22**。

本版内容：

- 修复 GitHub Pages 工作流仍调用 V0.15.1 固定 CI 的构建失败；
- GitHub Actions 改为运行 `python3 tools/ci_v0153.py .`；
- 前端版本、资源参数、Service Worker 与缓存门禁统一为 CACHE22；
- 保留 V0.15.2 的 B01 命格方案A、造化池、功法五槽、灵石升级、圆满、道卷兑换与突破历史小境界保护；
- 仓库中的 V0.15.2 功法迁移和升级后检查已替换为最终修正版。

部署时必须把 `.github`、`tools`、`assets` 和根目录文件一起上传到仓库根目录。
