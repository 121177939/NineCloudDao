# 九霄问道 V2.2.0 CACHE123 · 功法页签独立显示修复

## 目标

功法页三个页签严格互斥：

- **修炼**：只显示现有修炼功法、本命/专属修炼功法与修炼效果。
- **攻伐**：只显示当前攻伐槽、攻伐道藏10格及攻伐功法详情。
- **护体**：只显示当前护体槽、护体道藏10格及护体功法详情。

## 实现

CACHE122 原实现一次生成攻伐与护体两套DOM，再依赖 `hidden` 切换。CACHE123 改为**当前页签单家族渲染**：攻伐只调用 `pane('attack')`，护体只调用 `pane('defense')`；修炼页签清空并隐藏 `combatTechniqueRootV220`。同时增加 scoped `[hidden]{display:none!important}` 防回归。

## 数据库

生产已确认 `SQL254_GATE_PASSED`。本修复不改变数据库、功法数值、掉率、残卷、战斗结算或GM设置，**无需新增SQL**，下一数据库编号仍为 SQL255。
