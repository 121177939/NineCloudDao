# V2.0.10 CACHE102 升级说明

## 本次修复

1. **APP在线更新**：修复旧更新器在网络失败时仍提前写入30分钟节流、以及GitHub API版本不稳定的问题。新正式版冷启动优先检查；失败1分钟后可再次尝试；成功检查间隔10分钟。Release发布流程会校验APK、`app-update.json`、`SHA256SUMS.txt`三项附件。
2. **宗门英文内部码**：`AUTONOMOUS_CHOICE`、`cultivation`、`treasure`、`honor`、`personality`以及状态码统一转换为中文，未知内部码不再直接展示。
3. **人物经历按钮**：补齐人物档案弹层CSS与移动端布局，点击后会显示弟子最近事件、真实结算、重要记忆与故事线。
4. **弟子准备突破卡死**：修为圆满后直接给出突破入口。元婴及以上显示“等待宗主确认突破”，由宗主手动确认，避免无人值守触发永久死亡；低境界仍可自动尝试，也可手动立即结算。
5. **网页/APP同步**：本次修改已同步到Android内置`assets/game`，发布前会做共享文件SHA-256一致性校验。

## Android版本

- package：`com.jiuxiaowendao.game`
- versionCode：`2001102`
- versionName：`2.0.10-cache102`
- Release标签：`v2.0.10-cache102`

## 数据库 / GM

- 本次不新增SQL迁移，仍沿用SQL232；下一SQL编号保持233。
- GM仍为ADMIN9 R20，本次未修改。
- 若生产尚未执行SQL232，仍按SQL232包内PRECHECK → 安装 → POSTCHECK执行。

## 关于从V2.0.8没有收到更新提示

截图中的旧文案和原始`AUTONOMOUS_CHOICE`能够证明手机上实际运行的仍是V2.0.8内置资源。V2.0.8更新器本身存在“失败也提前进入30分钟静默节流”的缺陷，API兜底版本也不稳定；如果当前V2.0.8正好无法从GitHub读取更新，它无法被V2.0.10代码反向修复。此时需要**只做一次同签名覆盖安装V2.0.10正式Release APK（不要卸载APP）**。覆盖到V2.0.10后，后续版本继续走修复后的自动更新链。
