# 九霄问道 V2.1.1 CACHE112 / ADMIN9 R23 / SQL241

本次是“数据库稳定 + 客户端空转节流 + GM重做”统一更新。

- 客户端：选择性集成B-PERF2，不覆盖CACHE111整份app.js；只降低后台空转RPC，玩家眼前刷新和主动操作即时同步保持。
- 数据库：SQL241集成DBCAP03，以数据库权威滚动24小时治理技术历史，并增加保护条件、索引、分批和低频Cron。
- GM：ADMIN9 R23重新设计手机导航和数据库管理页，继续复用R22全部既有59个RPC接口。
- 发布：GitHub Pages默认Artifact R3、Android正式签名/Release R6链不变。

## 上线顺序

1. 生产备份 + 执行 `数据库管理_CACHE112/数据库健康检查_只读.sql` 保存上线前基线。
2. 执行SQL241升级SQL。
3. 部署CACHE112网页/Android，使用ADMIN9 R23。
4. R23数据库页一键检查 + dry-run；实机验收核心业务。
5. 执行SQL241制度门禁，必须返回 `SQL241_GATE_PASSED`。
6. 上线后1h/6h/24h/48h重复只读健康检查，比较容量斜率、Top10表、rollback、deadlock、temp I/O和清理失败数。

SQL241尚未在本制作环境连接生产数据库执行，因此不能把“文件制作完成”当作生产数据库已经升级成功。
