# V2.2.0 CACHE137 · SQL265 R2 进服热修

本热修**不改变 CACHE137 客户端**，只修复 SQL264 R3 对 `claim_cultivation_v1()` 的权威包装层在 `RETURNS TABLE` 环境下出现的 `character_id` 列/输出变量歧义。

错误现象：启动/修炼结算时报 `column reference "character_id" is ambiguous`，导致游戏初始化无法继续。

执行文件：`database-upgrades/SQL265_DESTINY_CULTIVATION_SPIRIT_ROOT/02_SQL265_R2_CACHE137_紧急热修_character_id歧义_恢复进游戏_执行即门禁.sql`。
成功标志：`SQL265_R2_HOTFIX_GATE_PASSED`。

保留：固定天道模式、正式 NPC 道侣最终修炼速度 ×1.5、CACHE137 修为榜灵根展示。
