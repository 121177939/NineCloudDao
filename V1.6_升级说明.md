# 九霄问道 V1.6 CACHE44 升级说明

## 版本

- 正式版本：`V1.6 CACHE44`
- 客户端构建：`v1-6-cache44`
- 开发基线：`V1.6_AB29_CACHE44`
- 下一正式SQL：`94`

## 性能改造

- 删除房间内每名玩家每秒调用推进RPC的轮询。
- 删除大厅每5秒执行维护与全量读取的轮询。
- 每个房间使用独立私有Realtime Broadcast频道；大厅使用独立私有频道。
- 普通房间、成员与玩家动作发送不含私牌的增量事件，前端直接局部合并状态。
- 开局、阶段切换、翻牌、结算等权威变化才读取一次完整快照。
- 大厅和房间快照函数改为严格只读，不再夹带踢人、关房或推进阶段。
- 单一数据库Cron每秒检查最多四间牌九房的到期阶段，并用咨询锁保证每个房间只推进一次。
- Realtime中断时，房间每60秒、大厅每120秒进行一次安全校准；本地倒计时到期只触发一次幂等推进兜底。

## 主游戏隔离

- 牌九继续运行在独立iframe中，不把React或完整Supabase SDK加入主游戏。
- 点击关闭牌九时，销毁Realtime连接、定时器与未完成同步，并把iframe切换为`about:blank`。
- 未打开牌九时不加载牌九实时运行时；关闭后牌九不再发送后台请求。
- 灵石飞行特效仅在牌九iframe内部本地播放，不写数据库、不广播动画帧。

## 安全边界

- 创建房间、入座、准备、抢庄、倍率、组牌、洗牌、发牌、资金冻结与结算继续使用PostgreSQL事务RPC。
- Broadcast只包含房间ID、版本、变化原因与安全公开字段；不包含牌堆、未公开骨牌、Service Role密钥或权威资金结果指令。
- 小牌九个人明牌与大牌九私牌仍只通过登录身份隔离的房间快照返回。
- V1.5全部牌九规则保持不变。

## 数据库执行

生产库已完成90号时依次执行：

1. `91_V1.6_九霄牌九事件驱动与主游戏隔离.sql`
2. `92_V1.6_CACHE44_正式发布门禁.sql`
3. `93_V1.6_CACHE44_升级后检查.sql`

93号所有`ok`必须为`true`后再部署前端。

## 验证边界

交付包完成源码静态检查、JavaScript语法检查、事件负载模型、历史功能回归和Pages产物验证。当前环境没有连接生产Supabase，因此Cron、Realtime频道、九名真实账号和生产延迟仍需上线验收。
