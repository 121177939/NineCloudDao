# 更新记录

## 0.6.6

- 新增 `release_config.json`，统一版本、缓存名、数据库基线、生产地址和发布文件清单。
- 新增 `tools/verify_release.py`，检查 JavaScript 语法、必需文件、版本一致性、HTML/Manifest/Service Worker 资源、敏感信息和命书最新 100 条逻辑。
- 新增 `tools/sync_release_metadata.py` 与 `tools/build_release.py`，从统一配置同步版本信息并自动生成完整游戏包和 GitHub 上传包。
- 新增数据库迁移登记表、发布验证说明、本地 8787 端口启动/关闭工具和真机复测清单。
- GitHub Actions 在部署前自动运行发布验证，验证失败则不部署。
- 命书由“最早 100 条”改为“最新 100 条”，按仙历年份和记录创建时间倒序显示，最新记录在最上方。
- 本版本不新增数据库表或函数，不执行 Supabase SQL，不修改玩家存档和既有玩法数值。

## 0.6.5

- 电脑端保持手机单栏结构，并随浏览器窗口宽度从 1.0 倍平滑放大到 1.5 倍。
- 修复电脑窗口变宽后界面仍固定在 760px 的问题。
- 功法页中的同名永久/限时持续加成按名称合并显示数量。
- 合并后的每秒修为与修炼倍率显示总和，例如“吐纳小悟 X4”。
- 不修改数据库与角色存档。

# 0.6.4

- 电脑端改为与手机端相同的信息结构，并按更大比例居中显示。
- 电脑端同样使用底部五栏导航，不再一次铺开全部模块。
- 命书改为读取并显示角色最早的 100 条人生记录。
- 同名功法获得记录合并显示，例如“吐纳小语 X1”“吐纳小语 X20”。
- 保留全部既有账号、角色、修为、功法效果、储物、机缘、突破与单设备登录规则。
- 本版本不需要执行新的数据库 SQL。
