# 九霄问道 V1.2 CACHE37 升级说明

## 升级基线

- 来源：V1.1 FIX1 CACHE36。
- 目标：V1.2 CACHE37。
- 历史正式SQL不回写；本次从64号继续追加。

## 核心规则

1. 角色主灵根为“变异灵根”时，根据原本五行显化：金→雷、木→风、水→冰。
2. 原金木水火土继续按现有函数参与克制，倍率完全不改。
3. 雷、风、冰不加入克制循环，只在最终伤害层增加8%。
4. 天生剑心仍需剑类武器与剑系功法，最终伤害增加8%。
5. 天生剑心与任何变异灵根互斥，绝不叠加。
6. 已有天生剑心后抽到变异灵根：候选变异灵根随机替换为其他合法灵根。
7. 已有变异灵根后抽到天生剑心：候选剑心随机替换为其他合法命格。
8. 升级前已经冲突的存量角色无法可靠判定先后，迁移时随机替换灵根或命格并写审计日志。
9. UI显示“变异灵根（雷/冰/风）”，仅属性字雷/冰/风分别使用紫色、蓝色、白色。

## SQL顺序

1. `64_V1.1_FIX1_安全随机兼容修复.sql`
2. `65_V1.2_升级前检查.sql`，所有检查必须为true。
3. `66_V1.2_变异灵根与剑心互斥.sql`
4. `67_V1.2_CACHE37_正式发布门禁.sql`
5. `68_V1.2_升级后检查.sql`，所有检查必须为true。
6. 覆盖完整前端包，等待GitHub Pages部署完成并确认客户端显示V1.2 CACHE37。

`69`只用于紧急停用变异伤害加成；`70`用于恢复。二者不会删除变异状态或撤销已完成的冲突替换。

## 重要说明

- 64号是V1.1 FIX1生产随机兼容修复，V1.2完整包将其补齐，重复执行安全。
- 变异灵根的修炼系数仍沿用现有灵根定义，本次不修改灵根抽取概率。
- 火、土暂不新增变异属性，也不获得雷风冰8%加成。
- 数据库不保存字体颜色；客户端按mutation_element渲染，避免数据污染。
