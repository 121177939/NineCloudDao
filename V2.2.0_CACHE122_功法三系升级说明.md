# 九霄问道 V2.2.0 CACHE122 · 功法三系升级说明

## 页面
功法页改为“修炼 / 攻伐 / 护体”。灵根与命格首页仅保留摘要，点击后弹出完整说明。攻伐与护体均为固定10格道藏，手机两列布局，直接显示残卷进度、参悟/层数/圆满状态。

## 玩法
现有功法全部归入修炼功法，原获取与概率不变。新增10本攻伐、10本护体，各装备1本，无参悟境界门槛、不赠送初始功法；天命榜、秘境PVP、秘境妖兽、世界BOSS全部生效。

## 残卷
攻防功法通过同名残卷获取，10张免费手动合成完整道卷；同品其它残卷可跨攻伐/护体混合5换1指定残卷，目标自身残卷不能作为兑换材料。秘境内未安全结算残卷不可合卷/置换。

秘境被玩家击败时，以本轮全部未结算功法残卷总数计算 `floor(总数×50%)`，再从败者真实残卷中随机原物转移。反对刷按胜者/败者有序账号对6小时计数：首次100%、第二次25%、第三次起0；全程留服务器审计账本。

## 获取
- 机缘：攻防残卷总概率 = 当前普通功法实际概率 ×20%，实时跟随生产普通功法掉率；同品先按攻伐/护体权重，再按单功法权重抽取。
- 秘境：独立掉率，玄12% / 地8% / 天4% / 仙2%，默认每次命中1张，禁止再次套“机缘五分之一”。
- 世界BOSS：沿用原每日前3次珍稀资格；普通20%天品，困难互斥池25%天品 +5%仙品 +70%无功法残卷，默认每次命中1张。
- 天命榜：功法真实生效，但V2.2.0第一版不产残卷。

## 升级
普通修炼、专属、攻伐、护体四类功法升级灵石默认全部×10。品级最高层保持黄6 / 玄12 / 地18 / 天24 / 仙30 / 专属36；默认每层效果成长10%、圆满效果×120%、圆满费用×150%。客户端只展示服务端权威费用，ADMIN9 R33可分别调整四类倍率与成长参数。

## 战斗
固定 `effect_code` + 参数快照服务端结算，中文功法名不参与逻辑判断。命中仍受5%~98%硬边界；破御采用相对削弱；护体减伤采用乘算且总减伤封顶80%。《九霄凌绝经》首回合0层、完整回合后叠层最多5；《玄水回元诀》每场首次存活跌入30%生机触发；圆满《九霄不灭身》每场首次致命伤保留1生机。

天命榜开战、秘境入场、世界BOSS锁定准备分别写入服务端功法快照。战报返回 `technique_effects`，GM真实接入检查会实际调用伤害函数验证增伤、减伤、凌绝叠层与不灭保命，不只检查字段存在。

SQL254为执行即门禁：天命榜/秘境必须证明真实B-COMBAT链，世界BOSS必须证明生产独立模拟器已接入同语义 `effect_code` 与准备快照；任一真实战斗链不满足即整笔回滚，成功末尾必须看到 `SQL254_GATE_PASSED`。

## 高阶界闻
服务器首次有人参悟某本天品/仙品攻防功法时，SQL254会尝试向既有九霄界闻写入“天地有感”。该写入采用结构兼容检查与失败静默，不参与核心SQL门禁，避免旧服界闻字段差异阻断功法主链。

## SQL254 R5 生产掉率与批量机缘修正

SQL254 R1 因秘境直接调用门禁误判回滚；R2 因世界BOSS函数 dollar-quote 结束分号遗漏而解析失败；R3 修正语法和三条真实战斗链后，在最终 Gate 发现普通功法掉率结构无法解析并主动回滚。R1/R2/R3 均不能记为 SQL254 ONLINE。

用户只读诊断确认生产 `opportunity_v4_technique_drop_rates` 字段为 `grade/main_rate/support_rate`，原机缘普通功法判定使用同一随机数，实际总概率为 `main_rate+support_rate`。R5继续沿用这一真实结构，并在用户确认后进一步修正黄品规则：
- 攻防残卷机缘概率严格使用 `(main_rate+support_rate)×20%`；
- 黄品因生产表无普通功法行，不伪造旧普通功法概率；新攻防黄品残卷在黄品趋吉机缘内独立5%，GM可调；
- 机缘包装器按本次真实 `settlement_batch_id` 遍历全部 `opportunity_v3_results`，在线/离线批量结算逐条独立判定，不再依赖返回JSON的少数结果；
- 保留 `settle_opportunity_v4(boolean default true)` 的原RPC默认参数；
- 最终门禁校验生产掉率值、黄品回退和批量机缘适配器。

V2.2.0 正式上线必须执行 SQL254 R5 并看到 `SQL254_GATE_PASSED`；不要再执行 R1 / R2 / R3。

## CACHE122 / SQL254 R5 概率澄清

- 旧普通修炼功法掉率不改。生产普通功法条件概率仍为玄4.1667%、地10%、天25%、仙50%，这些不是所有机缘的总概率。
- 新攻防残卷：黄品趋吉机缘内独立5%；玄/地/天/仙为对应普通功法条件概率的20%。
- 玩家功法详情与ADMIN9 R33明确标注“对应品级趋吉机缘内”的条件概率，避免把仙品50%误解为全局50%。
