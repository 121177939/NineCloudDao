# 发给B先读

当前正式基线：`V1.7.8 CACHE58`。
开发锁：`V1.7.8_AB43_CACHE58`。
下一正式版本：`V1.7.8`；下一SQL：`117`。

牌九正式视觉已经进入 `B-PAIGOW02-UI03-RUNTIME-PARITY`。后续B候选不得以旧HTML或旧PATCH_ROOT覆盖正式页面；所有视觉变更必须适配正式 `paigow-app.js` 的动态状态，并由A线最后移植。
