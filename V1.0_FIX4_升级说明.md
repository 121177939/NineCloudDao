# 九霄问道 V1.0 FIX4 CACHE34 升级说明

前置基线：**V1.0 FIX3 CACHE33**。

## 本次安全修复

- 大堂“灵骰问道”“气运龟卜”与“鱼虾灵局”单次下注不得超过当前可用灵石或修为的10%。
- 玩家庄取消荷老系统兜底，任何新结算的 `system_cover_amount` 均为0。
- 玩家庄即时局在开奖前按最高可能赔率校验并锁定赔付能力；余额不足时拒绝开局。
- 鱼虾灵局玩家庄在落注时真实预扣3倍最大赔付保证金，结算后退回未使用部分。
- 玩家庄有效局统一收取5%坊税进入现有全服造化池：
  - 闲家押100失败：庄家实得95，5进入灵石造化池。
  - 闲家押100普通胜利：返还本金100，净利润95，5进入灵石造化池；庄家承担完整100赔付。
- 大堂与鱼虾下注增加唯一 `request_id`。重复提交同一请求只返回首次结果，不会重复扣款、开奖或派奖。
- 旧结算RPC撤销客户端执行权限，防止绕过10%限制和幂等保护。
- 新增请求审计表，记录下注参数、结算前后资源、结果与结算版本。
- 不修改灵骰、龟卜或鱼虾灵局的开奖概率、普通倍率和天命豹子倍率。

## 升级顺序

1. 执行 `44_V1.0_FIX4_升级前检查.sql`。
2. 执行 `45_V1.0_FIX4_赌坊安全结算.sql`。此步骤会立即撤销旧赌坊结算RPC，建议随后立刻部署前端。
3. 解压完整部署包，全部覆盖GitHub仓库根目录并推送 `main`。
4. 等待Pages工作流 `python3 tools/ci_v1_fix4.py .` 全部通过并发布完成。
5. 执行 `46_V1.0_FIX4_CACHE34_正式发布门禁.sql`。
6. 执行 `47_V1.0_FIX4_升级后检查.sql`，所有 `ok` 应为 `true`。

出现异常时可执行 `48_V1.0_FIX4_紧急停用玩家庄.sql`。该脚本只关闭玩家坐庄，荷老系统庄仍可通过安全RPC继续运行。

## 旧鱼虾玩家局处理

升级执行时仍未结算、且没有预扣赔付保证金的旧玩家鱼虾下注会作废并原数退还闲家，避免旧局继续触发系统兜底。已结算历史记录不会被改写。

## 不变规则

- 全局渡劫成功率80%硬上限不变。
- 灵石继续统一存储于 `character_inventory`。
- 系统庄现有造化池规则、灵骰概率与赔率不变。
- 贵宾雅间规则不在本次修改范围内。
