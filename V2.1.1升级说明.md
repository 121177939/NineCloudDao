# 九霄问道 V2.1.1 CACHE107 升级说明

本版是 V2.1.0 候选的修正版；V2.1.0 未完成数据库门禁与Pages部署，不作为正式上线基线。

## 版本
- Web：V2.1.1 CACHE107
- Build：`v2-1-1-cache107-wboss01-equipmentforge1-admin21-sql235-readyfix1`
- Android：versionCode `2001407` / versionName `2.1.1-cache107`
- DB：SQL233 R3；下一编号234
- GM：ADMIN9 R21

## V2.1.1修复
1. 修复GitHub Pages build：旧V2.1.0仓库虽然客户端已改到CACHE105，但Pages构建器仍硬校验 `V2.0.12 CACHE104`、`cacheEpoch:104`、SQL232，因此在build阶段exit code 1。
2. 新建 `tools/build_pages_v2_1_1.py`，按V2.1.1/CACHE107/SQL233真实基线校验。
3. Pages产物补入 `b-equipment-v210.js/css`；工作流JS语法检查同时覆盖世界BOSS与装备V2.1模块。
4. 保持已验证Pages发布方式不变：默认 `github-pages` Artifact R3 + `actions/deploy-pages@v4`，只修版本门禁和产物清单。
5. SQL233升级为R3结构门禁：`jiuxiao_app_release_control` 仅作为发布显示状态。用户真实库观测到该表仍为 `V2.0.3 CACHE95/95`，R3不再要求它必须显示V2.0.12/CACHE104。
6. SQL233 R3改为核验SQL232标记函数/触发器、关键装备表字段、战斗/强化RPC、元婴 `major_order=4`、旧三种洗练物品与SQL233冲突；数据库若真未达到SQL232会明确停止。
7. SQL233安装阶段不修改release/cache、不开启世界BOSS；生产 `PRODUCTION_ENABLE` 全部通过后才统一写为V2.1.1 CACHE107并开启BOSS。

## 上线顺序
测试库：`R3 PRECHECK → SQL233 R3 INSTALL → R3 POSTCHECK → R3 TEST_ENABLE → 完整验收`。

生产：重新 `PRECHECK → INSTALL → POSTCHECK`，客户端V2.1.1部署完成后，最后执行 `PRODUCTION_ENABLE_SQL233_V2.1.1_R3_正式发布门禁.sql`。
