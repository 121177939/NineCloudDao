# 九霄问道 V2.2.0 CACHE130 · 天道人物交互响应与移动端性能修复

## 生产基线

- 游戏：V2.2.0 CACHE130
- Android：2001509 / 2.2.0-cache130
- GM：ADMIN9 R37
- 数据库：SQL259 ONLINE / NEXT SQL260
- Edge Function：`tiandao-ai` 已上线；本次无需重新部署

## 根因与修复

### 1. 人物详情打开后无法关闭
CACHE129 在 `render()` 时绑定弹窗按钮，但人物详情是在之后异步插入 `#modalRoot` 的，因此关闭、交谈、赠礼、相约等动态按钮没有事件。CACHE130 改为文档级事件委托，并加入遮罩点击与 Esc 关闭。

### 2. 人物页移动端卡顿
CACHE129 的 MutationObserver 监听整个 `#app` 子树并对每次变化执行 `mount()`。人物模块自己的 render 也会产生大量 mutation。CACHE130 仅在主程序真正新增/替换 `#npcSocialSection` 时重新挂载，并通过 requestAnimationFrame 合并执行。

### 3. NPC交互像“没反应”
Cloudflare 人格决策通常需要数秒。CACHE129 点击后没有任何即时状态。CACHE130 点击后立即显示“NPC正在思量/回应”，禁用重复提交；人物详情读取也先显示加载弹层并启用45秒短缓存。Cloudflare失败或超时仍由现有 `server_personality_v1` 自动 fallback。

## 不变项

- 不修改 SQL259。
- 不删除、不替换 `server_personality_v1`。
- 不把 Cloudflare Token / Account ID 写入客户端。
- 不改变 `tiandao-ai` 服务端审核边界。
