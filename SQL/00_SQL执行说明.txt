九霄问道 V1.0 CACHE30 SQL执行说明

可从 V0.15.4 FIX5 CACHE25 直接升级；不要求补跑 CACHE26/CACHE27 的纯发布门禁 SQL。

推荐正式发布顺序：
1. 执行 26_V1.0_升级前检查.sql（只读；所有 ok 应为 true）。
2. 执行 27_V1.0_BCOMBAT01_五行战斗与越阶挑战.sql（正式主迁移）。
3. 覆盖并发布 V1.0 完整前端包，等待 GitHub Pages 构建成功。
4. 执行 28_V1.0_CACHE30_正式发布门禁.sql（让旧客户端切换至 CACHE30）。
5. 执行 29_V1.0_升级后检查.sql（只读；所有 ok 应为 true）。

应急文件：
- 30_V1.0_战斗挑战紧急停用.sql：只停止新挑战，不删除数据。
- 31_V1.0_BCOMBAT01_完整回滚.sql：删除本模块表和战报，属于破坏性完整回滚；只在确认回滚前端后执行。

注意：先在备份/测试库验证。不要一次性执行整个 SQL 文件夹。
