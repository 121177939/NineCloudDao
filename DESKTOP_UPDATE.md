# 桌面端更新说明 · V0.14.4 CACHE8

1. 上传完整V0.14.4源码后等待GitHub Pages构建完成。
2. 先执行V0.14.4游戏SQL，再执行ADMIN2 SQL；ADMIN2 HTML只在本地使用，严禁公开部署。
3. 新版本会通过CACHE8自动清理旧缓存；如仍显示旧版，完全关闭页面后重新打开。
4. 线上数据库执行状态必须由Supabase SQL Editor结果确认，交付包不等于已部署。
