# V0.6.4 GitHub Desktop 更新

1. 打开 GitHub Desktop，确认仓库是 `NineCloudDao`、分支是 `main`。
2. 点击顶部 `Fetch origin`；若出现 `Pull origin`，再点击一次。
3. 点击 `Show in Explorer`，打开 GitHub Desktop 真正监控的本地仓库。
4. 解压本更新包，进入能直接看到 `index.html`、`app.js`、`styles.css` 的位置。
5. 复制更新包内部全部文件和文件夹到本地仓库根目录，选择“替换目标中的文件”。
6. 回到 GitHub Desktop，Summary 填写：`Update game to V0.6.4`
7. 点击 `Commit to main`。
8. 点击顶部 `Push origin`。
9. 在 GitHub 仓库的 Actions 中等待绿色对勾。
10. 访问 `https://121177939.github.io/NineCloudDao/?v=064`。

本版本不需要执行新的 Supabase SQL。
