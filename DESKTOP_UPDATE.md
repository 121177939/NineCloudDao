# GitHub Desktop 更新到 V0.6.5

1. 打开 GitHub Desktop，选择 `NineCloudDao` 和 `main`。
2. 点击 `Fetch origin`；出现 `Pull origin` 时再点击一次。
3. 解压本更新包，进入能直接看到 `index.html` 的文件夹。
4. 在 GitHub Desktop 点击 `Show in Explorer`。
5. 把更新包内全部文件复制到仓库目录，选择替换。
6. Summary 填写 `Update game to V0.6.5`。
7. 点击 `Commit to main`，再点击 `Push origin`。
8. 等待 GitHub Actions 绿色对勾，打开 `https://121177939.github.io/NineCloudDao/?v=065`。

本版不需要执行 Supabase SQL。
