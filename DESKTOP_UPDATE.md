# 使用 GitHub Desktop 更新 V0.6.2

1. 打开 GitHub Desktop，确认仓库是 `NineCloudDao`、分支是 `main`。
2. 点击 `Fetch origin`；若变成 `Pull origin`，再点一次。
3. 点击 `Show in Explorer` 打开本地仓库。
4. 解压本上传包，进入到能直接看到 `index.html` 的位置。
5. `Ctrl + A` 复制包内全部内容，粘贴到本地仓库根目录，选择“替换目标中的文件”。
6. 回到 GitHub Desktop，在 Summary 填：`Update game to V0.6.2`。
7. 点击 `Commit to main`。
8. 点击顶部 `Push origin`。
9. 打开 GitHub 仓库的 Actions，等待绿色对勾。
10. 手机刷新正式网址；若仍是旧界面，关闭网页后重开，或清除该网站缓存。
