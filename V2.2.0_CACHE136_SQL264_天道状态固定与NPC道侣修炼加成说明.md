# V2.2.0 CACHE136 / SQL264 / ADMIN9 R40

本次只改三项正式规则：

1. ADMIN9 R40 在“天道人物”页新增“天道修炼均衡状态”，支持自动动态均衡、固定大道均衡、固定天道福泽、固定天道阻滞；福泽/阻滞倍率可分别配置。固定模式由服务端 `get_heaven_balance_v1()` 权威入口执行，不是客户端假显示。
2. 玩家与天道人物 NPC 形成 `active` 正式道侣后，SQL264 自动写入 `NPC道侣同修` 修炼效果，倍率 +50%（基础 1/秒时为 1.5/秒）；道侣 `ended` 或记录删除后自动失效。SQL264 会回填当前已经存在的 active 道侣。
3. 修炼首页和“修炼速度构成”详情新增 `NPC道侣同修 ×1.50` 显示；关系状态变化时客户端会刷新云端修炼效果。

部署顺序：先执行 SQL264 并看到 `SQL264_GATE_PASSED`，再部署 CACHE136 Pages/客户端；GM 使用 ADMIN9 R40。Edge `tiandao-ai CACHE135 R6` 无需重部署。


> **已由 SQL264 R2 取代。不要再执行 SQL264 R1。R1 的错误预检会触发 SQL264_PRECHECK_HEAVEN_BALANCE_NOT_IN_SETTLEMENT_CHAIN 并回滚。**
