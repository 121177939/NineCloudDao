# APP V2.0.8 未收到更新提示：诊断与一次性恢复

用户截图中仍出现“下次人物事件”“下次自主决定”与原始 `AUTONOMOUS_CHOICE`，这些字符串只存在于V2.0.8代码，说明手机实际运行的不是V2.0.9内置资源。GitHub页面显示V2.0.9 Release存在，并不能证明V2.0.8客户端成功读取了Release清单。

V2.0.8更新器有两个薄弱点：
- 网络请求开始前就写入30分钟节流；本次请求即使失败，之后也会静默等待。
- GitHub API兜底使用非稳定API版本；直链失败时兜底可能继续失败，而自动检查错误又默认不弹窗。

V2.0.10已改为：冷启动第一次优先检查；失败后1分钟可重试；只有成功读取Release后记录成功检查；稳定API版本`2022-11-28`；HTTP禁用缓存；Actions发布后强制核对三项更新附件。

**旧V2.0.8无法通过尚未安装的新代码修复自己。** 如果它继续不弹提示，请从GitHub Release下载V2.0.10正式签名APK直接覆盖安装。不要卸载旧APP，必须保持同包名与同签名，才能保留应用数据并验证签名连续性。

若覆盖安装提示签名不一致，立即停止，不要卸载；说明手机现装APK并非同一永久签名链，需要先核对Release签名来源。
