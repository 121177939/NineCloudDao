# V2.1.1 CACHE118 · GitHub Pages 构建修复

## 根因
CACHE117源码与VERSION已经升级，但 `.github/workflows/deploy-pages.yml` 仍写死 CACHE116：

- Verify source files 仍要求 `V2.1.1 CACHE116`；
- 仍调用 `tools/build_pages_v2_1_1_cache116.py`；
- 线上部署验收也仍检查 CACHE116。

因此上传CACHE117后会在build阶段直接 exit 1。截图中的Node.js 20 deprecated为warning，不是此次失败原因。

## CACHE118修复
- 新增 `tools/build_pages_v2_1_1_cache118.py`。
- Pages workflow从源码检查、构建器、线上VERSION/index验收全部统一为CACHE118。
- 保持已验证的 default github-pages artifact R3 / deploy-pages 发布方式不变。
- 完整继承CACHE117装备位置实时确认与SQL247升品/破境修复。
- SQL247 R2已上线；CACHE118无新增SQL，下一编号SQL248。
