# 九霄问道 V2.2.0 CACHE131 · 完整天道人物升级说明

## 本版目标

CACHE131 不再把天道人物做成“交谈 / 赠礼 / 相约”三个按钮的关系刷取器，而是把人物改成会生活、会主动联系、会经历事情、会记住承诺与共同经历的长期人物系统。

## 玩家侧完整玩法

- **每日生活**：已认识人物拥有当天心情、活动、地点、需求与可谈话题；同一人物每天呈现不同状态。
- **主动来信**：NPC 可以主动传音；玩家可以认真回复、去见TA或晚些再说。NPC不再只等玩家点击。
- **人生事件**：人物会出现自己的事件线，按阶段推进；玩家可以帮忙、倾听、保持距离、守诺、坦白、反悔、支持、建议、陪TA面对结果。
- **NPC会自己继续生活**：事件有期限，玩家长期不介入时会由NPC自行走到结果，而不是永久停在等待玩家。
- **承诺系统**：主动答应帮忙会形成真实承诺与期限；守诺提高信任/敬重并留下长期记忆，失约会产生永久的关系后果。
- **情境交谈**：询问近况、认真倾听、分享经历、打趣、请教，以及自由输入交谈；自由文本通过现有 `tiandao-ai` 处理。
- **情境赠礼**：实用、修行、雅趣、珍奇四类礼物；NPC有真实偏好，匹配偏好效果更好；不接受礼物的人可以直接拒收且不会扣灵石。
- **情境相约**：坊市、茶楼、山顶、修炼、同行等不同方式；人物性格与价值取向影响相约效果。
- **关系画像**：玩家界面以“亲近/信任/熟悉/敬重/亏欠/紧张”等文字呈现，不把人物简化成一根好感度条。
- **重要记忆 / 小片段分层**：高价值共同经历单独保留；旧的完全重复低重要度流水账会去重，新的相同记录也有时间窗去重。
- **秘密与人生阶段**：信任和亲密达到条件后，人物会逐步透露长期目标/隐秘；人生事件解决后其人生阶段会推进。
- **仙缘 / 道侣继承并扩展**：表白仍由服务端审核；道侣支持自由传音、不同礼物、不同相约、共同修炼与护道，人物依然保持自己的生活状态。

## AI与安全边界

- 继续复用已上线的 Supabase Edge Function：`tiandao-ai`，**本版无需重新部署 Edge**。
- Cloudflare Workers AI 只负责人格、对话、下一目标和行动意图提案。
- 灵石、关系数值、事件推进、承诺、道侣成立等全部由 PostgreSQL 服务端规则二次审核。
- Cloudflare失败、超时或GM关闭AI时，永久自动回退 `server_personality_v1`。
- Cloudflare Token / Account ID 不进入网页、Pages、Android客户端。


## SQL260 R2 兼容热修

SQL260 R1 在替换 `tiandao_add_memory_v259(...)` 时错误改变了 SQL259 线上既有函数的返回类型（`void` → `uuid`）和参数名，PostgreSQL 会在执行阶段报 `42P13`。R2 已保持 SQL259 原函数 ABI（`p_type` + `RETURNS void`），仅升级函数体加入记忆去重，并保留原有低重要度记忆50条上限；不使用 `DROP FUNCTION`，避免影响既有依赖。

如果 R1 已报错，没有看到 `SQL260_GATE_PASSED`，不要部署 CACHE131。直接执行 R2 全文件；如 SQL 编辑器提示事务处于 aborted 状态，先执行 `rollback;`。

## 数据库

新增 **SQL260**。主要加入：

- `tiandao_life_state_v260`
- `tiandao_story_threads_v260`
- `tiandao_inbox_v260`
- `tiandao_promises_v260`
- 关系新增 `respect / promise_kept / promise_broken`
- 重写人物 Hub / Detail 只读RPC
- 重写现有 `tiandao_ai_prepare_v259 / tiandao_ai_apply_v259` 的内部逻辑，因此现有 Edge Function 可以直接兼容 CACHE131。

SQL260 最后必须返回：

`SQL260_GATE_PASSED`

未看到该结果时不要部署 CACHE131 客户端。

## GM

GM 升级为 **ADMIN9 R38**，新增完整天道人物总开关、每日生活、NPC主动、人生事件、自由交谈、刷新周期、最大事件数、最大未读来信、事件期限、承诺期限、记忆去重窗口，以及生活/事件/来信/承诺实时监控。

## 发布顺序

1. 当前生产继续保持 CACHE130 / SQL259 ONLINE，先备份数据库。
2. 执行 **SQL260 R2**，确认最后看到 `SQL260_GATE_PASSED`。
3. 部署 CACHE131 的预构建 `pages/` 或完整 GitHub 仓库。
4. Android 使用 CACHE131 工程重新构建正式签名 APK；版本 `2001510 / 2.2.0-cache131`。
5. GM 使用 ADMIN9 R38。
6. **不要重新部署 `tiandao-ai` Edge Function。**

## 离线验收

已通过：全部运行时JS语法、Pages构建门禁、Web/Android共享资源同步、Android XML解析、Gradle Wrapper JAR完整性、GM脚本语法/重复ID、客户端Cloudflare秘密扫描、SQL260结构/门禁静态检查。

由于当前交付环境未连接你的生产PostgreSQL，也没有Android SDK与正式签名Secrets，所以 SQL260 的真实执行结果和正式APK构建必须在你的生产/构建环境完成。
