# 九霄问道 V2.5.0 CACHE141 · 批量分解弹窗滚动与确认按钮修复

- 问题：手机端批量分解候选装备较多时，弹窗整体超过可视高度，底部「确认分解」被遮挡且页面无法继续下拉。
- 修复：将装备列表与风险说明放入独立滚动区；确认分解操作区保持在弹窗底部。
- 手机端弹窗按 `100dvh` 与 `safe-area-inset-bottom` 限高，兼容 Android WebView/PWA 底部系统区域。
- 沿用 CACHE140 的「一键选择地品」逻辑，不修改 `decompose_equipment_v180`，无新增 SQL。
- 数据库：SQL267 ONLINE；SQL268 R1 状态沿用现有基线。
