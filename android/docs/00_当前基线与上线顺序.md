# 当前基线与上线顺序 · V2.2.0 CACHE124

1. 生产数据库已确认 **SQL254 R5 ONLINE / SQL254_GATE_PASSED**。
2. 执行 **SQL255**，必须看到 `SQL255_GATE_PASSED`；失败则整笔回滚，不得发布本客户端。
3. SQL255成功后发布 **V2.2.0 CACHE124** Pages / Android，并使用 **ADMIN9 R34**。
4. SQL255成功后的下一数据库编号为 **SQL256**。
5. Pages继续使用已验证的 GitHub Actions default Pages artifact R3；Android继续使用既有正式签名和 Release 链，不改变包名和签名流程。
