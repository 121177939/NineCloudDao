# 当前基线与上线顺序 · V2.2.0 CACHE122

1. 生产数据库已确认 SQL252 成功。
2. 若 SQL253 尚未执行：先执行 SQL253，必须看到 `SQL253_GATE_PASSED`。
3. 执行 SQL254，必须看到 `SQL254_GATE_PASSED`；任何前置战斗链接入不足都会整笔回滚。
4. 再发布 V2.2.0 CACHE122 Pages / Android，并使用 ADMIN9 R33。
5. Pages 继续使用已验证的 GitHub Actions default Pages artifact R3；Android继续使用既有正式签名和 Release 链。
