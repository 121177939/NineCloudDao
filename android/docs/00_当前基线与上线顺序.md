# 当前基线与上线顺序 · V2.2.0 CACHE133

1. 当前已知生产数据库：**SQL260 ONLINE**。
2. 先执行 **SQL261**，必须看到 `SQL261_GATE_PASSED`。
3. 重部署 Supabase Edge Function **`tiandao-ai` CACHE133 R2**；Cloudflare Secrets 名称不变，`server_personality_v1` fallback 永久保留。
4. 部署 CACHE133 Pages / 游戏仓库。
5. Android 使用 **versionCode 2001512 / versionName 2.2.0-cache133**，再运行 `release-apk.yml`。
6. GM 继续 **ADMIN9 R38**；下一数据库编号 **SQL262**。
