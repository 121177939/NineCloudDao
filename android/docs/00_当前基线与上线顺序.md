# 当前基线与上线顺序 · V2.2.0 CACHE132

1. 生产数据库：**SQL260 ONLINE / SQL260_GATE_PASSED**，下一条 SQL261。
2. Supabase Edge Function `tiandao-ai` 已上线；Cloudflare Workers AI 与 `server_personality_v1` fallback 继续复用。
3. CACHE132 只修复 PC 底部导航鼠标横向拖拽、滚轮/触控板分页，**无需新增 SQL，也无需重部署 Edge Function**。
4. Android 使用 **versionCode 2001511 / versionName 2.2.0-cache132**；GM 继续 **ADMIN9 R38**。
