# SQL255 R4 执行说明 · V2.2.0 CACHE125 / 天墟

生产已确认 SQL254_GATE_PASSED。SQL255 R1/R2/R3 均在 COMMIT 前失败并回滚，不能视为上线。

当前应执行：`01_SQL255_R4_V2.2.0_CACHE124_赌场退役_天墟自由交易_Cron_API兼容_执行即门禁.sql`。

R4 保留表级装备托管保护，并使用 pg_cron API 兼容方式退役赌场任务，不直接 UPDATE cron.job。

只有最终出现 `SQL255_GATE_PASSED` 才能把 SQL255 记为 ONLINE；成功后 NEXT SQL256。

CACHE125 是 Pages 部署链修复版，不改变 SQL255 的数据库业务规则。
