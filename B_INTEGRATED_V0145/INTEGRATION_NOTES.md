# A线接入说明

- 用户已明确批准修改保护区“玩家庄零抽成”为“赢家毛利润5%归庄家”。
- 机缘SQL依赖V0.11.5的`award_opportunity_technique_v3`、`award_cave_resource_v3`与V0.13.0修为上限函数。
- 不得只执行前端补丁；数据库结构、内容池与结算函数必须同步部署。
- 旧机缘V4 MODULE2包已作废，不可与本包重复执行。
- `source/candidate`仅供比对，A线应应用精确patch，不得整文件盲覆盖。
- 真实数据库未在B线环境执行，必须验证函数编译、RLS、双端并发、864次离线性能。
