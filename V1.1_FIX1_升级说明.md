# 九霄问道 V1.1 FIX1 CACHE36 升级说明

## 升级目标

本版在 V1.1 CACHE35 上重构万运博弈楼的资金与开奖模型，保留现有造化池余额和旧等权奖票规则，不设置每日场次或连续游玩次数限制。

## 已锁定规则

### 两小时赌场周期

- 每期长度固定为 2 小时。
- 每期结束前 1 分钟停止全部赌场新下注；已受理赌局继续完成结算。
- 灵石赌场每期期初资金固定重置为 `100,000,000`。
- 修为赌场每期期初资金固定重置为 `1,000,000,000`。
- 每期独立核算，不追补历史亏损。
- 系统重置注资或回收不计入本期利润。

### 利润与造化池

- 保留升级前的全部灵石、修为造化池余额。
- 赌场本期利润大于 0 时，将利润的 50%加入对应造化池。
- 本期利润小于或等于 0 时，不新增奖池资金，也不开奖；旧池余额全部滚存。
- 开奖仍按旧规则在本期有效角色中等权抽取一人；重复游玩不增加个人抽中权重。
- 获奖者领取开奖前奖池的 70%，其余至少 30%滚存。

### 系统庄公开概率与赔率

| 玩法 | 公开概率 | 押100中奖净利润 |
|---|---:|---:|
| 灵骰·大 | 105/216，豹子除外 | 95 |
| 灵骰·小 | 105/216，豹子除外 | 95 |
| 灵骰·任意豹子 | 6/216 = 1/36 | 3320 |
| 气运龟·平 | 50% | 90 |
| 气运龟·吉 | 25% | 280 |
| 气运龟·凶 | 25% | 280 |
| 鱼虾灵局命中1枚 | 75/216 | 106 |
| 鱼虾灵局命中2枚 | 15/216 | 210 |
| 鱼虾灵局命中3枚 | 1/216 | 320 |

灵骰改为服务器直接生成三颗独立公平骰：任意豹子通吃大小，玩家必须独立选择“豹子”才能中奖。随机数改为 `gen_random_bytes()` 拒绝采样，不再使用普通 `random()` 结算高价值玩法。

### 下注上限与并发

- 不限制每日场次、不设置玩法冷却。
- 即时大堂每局下注不得超过该局扣款前可用资源的 30%。
- 鱼虾灵局在同一个 40 秒公共轮次内，所有法印、系统庄/玩家庄下注合计不得超过首次落注时可用资源的 30%。
- 同一角色同时只能有一笔即时结算请求；并发请求直接拒绝，不再排队批量结算。
- 唯一 `request_id` 继续保证同一请求只扣款、开奖、派奖一次。

### 玩家庄

- 玩家庄采用 `100:97.5`：获胜方取得名义利润的 97.5%，2.5%平台费进入灵石赌场资金。为保证整数账户精确结算，玩家庄下注按40灵石步进。
- 玩家庄不直接向奖池划款；平台费计入赌场本期利润，周期末正利润的50%才进入奖池。
- 庄家承担全部名义赔付责任；鱼虾灵局落注时冻结3倍最大责任。
- 系统庄绝不兜底；庄家资金不足时下注直接拒绝。

## SQL执行顺序

1. `58_V1.1_FIX1_升级前检查.sql`
2. `59_V1.1_FIX1_赌场周期资金与公平结算.sql`
3. `60_V1.1_FIX1_CACHE36_正式发布门禁.sql`
4. `61_V1.1_FIX1_升级后检查.sql`

应急脚本：

- `62_V1.1_FIX1_紧急停用赌坊.sql`
- `63_V1.1_FIX1_恢复启用赌坊.sql`

58号检查会拒绝在仍有 `pending` 请求或未结算鱼虾下注时升级。建议先停止前端新流量，等待当前鱼虾轮次自然结算后再执行。

## 部署顺序

1. 备份生产数据库及当前网站。
2. 执行58号只读检查。
3. 执行59号正式迁移。
4. 执行60号发布门禁。
5. 执行61号只读检查，确认全部为 `true`。
6. 覆盖部署本完整包的前端文件。
7. 清缓存并验证客户端显示 `V1.1 FIX1 CACHE36`。

数据库迁移未在本地生产库执行；上线前必须以生产环境58号、61号输出为最终依据。
