# 九霄问道 V2.2.0 CACHE129 · 天道人物 Cloudflare Workers AI 正式接入

## 正式版本

- 游戏：**V2.2.0 CACHE129**
- Android：**versionCode 2001508 / versionName 2.2.0-cache129**
- GM：**ADMIN9 R36**
- 数据库：生产当前确认 **SQL258 ONLINE**；本版执行 **SQL259 R2**，成功后 NEXT SQL260
- Edge Function：`tiandao-ai`
- Cloudflare Workers AI：`@cf/qwen/qwen3-30b-a3b-fp8`

SQL259 R2 正式替代此前尚未确认上线的 SQL259 R1。不要先把 SQL259 R1 标记为 ONLINE。

## 正式链路

玩家端只负责提交人物动作：

`玩家端 → Supabase Edge Function tiandao-ai → Cloudflare Workers AI → AI提案 → PostgreSQL tiandao_ai_apply_v259 服务端复核 → 游戏状态写入`

Cloudflare AI 只能给出：人格判断、对话、下一目标、`accept / defer / reject`、NPC行动意图。

Cloudflare AI **没有**数据库凭据，也不能决定或直接修改玩家灵石、装备、境界、NPC境界、战斗胜负、正式道侣关系或任意数据库字段。灵石消耗、关系增减、冷却、缘遇结算、表白门槛和正式结为道侣全部由数据库规则重新校验后执行。

## Fallback

`server_personality_v1` 已成为真实可调用的服务端人格引擎并保留：

`Cloudflare Workers AI → 失败 / 超时 / Secret未配置 / GM关闭AI → server_personality_v1 → 同一服务端审核函数`

Fallback 与 Cloudflare 共用同一条状态写入规则，不存在“Fallback绕过审核”的第二条写入链。

AI返回的“下一目标 / 行动意图”只写入独立的 `tiandao_ai_state_v259` 白名单叙事状态，下一次人格判断会把它作为上下文继续使用；该表不保存资产、境界、战斗或正式道侣权威状态。

## SQL259 R2 安全边界

新增两阶段请求票据：

1. `tiandao_ai_prepare_v259`：验证登录角色、人物、动作、关系、冷却和表白前置条件，生成短时请求票据与 AI 上下文。
2. Edge Function 调用 Cloudflare 或 fallback，仅得到提案。
3. `tiandao_ai_apply_v259`：锁定票据并再次验证状态，以固定服务端公式决定实际关系变化、资产消耗和是否允许正式结缘。

旧的四个直接写 RPC 仍保留历史定义，但已撤销 `authenticated` 执行权：
`resolve_tiandao_encounter_v1`、`tiandao_npc_interact_v1`、`tiandao_romance_action_v1`、`tiandao_companion_action_v1`。
客户端 CACHE129 也不再引用它们。

## GM ADMIN9 R36

「天道人物」增加：

- AI状态：Cloudflare / 本地Fallback
- 测试AI连接
- 最近AI决策
- AI耗时
- AI失败原因
- 当前使用模型
- AI启用开关、超时和最大输出 Token

连接测试成功应显示：

Cloudflare Workers AI：连接成功
模型：@cf/qwen/qwen3-30b-a3b-fp8
NPC人格决策：正常
fallback：可用

## Secret 规则

Cloudflare Account ID 和 API Token 都由 Edge Function 从 Supabase Secrets 读取。API Token 的实际值不进入任何交付源码。

公开游戏运行资源 `config.js / app.js / Pages / Android assets` 不包含 Cloudflare Secret 名称或凭据。公开 GitHub 交付包也不包含服务端 `supabase/` 私有部署目录；Edge Function 另以服务端私有包交付。
