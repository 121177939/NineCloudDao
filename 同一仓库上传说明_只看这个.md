# V2.0.6 CACHE98：同一仓库上传与APP发布

## 网页

1. 在当前仓库 Actions 中取消仍在运行或排队的旧 Pages 工作流。
2. 解压本包，把最外层文件覆盖到当前仓库 `main` 根目录。不要新建仓库，不要放进子文件夹。
3. 确认仓库只保留一个网页工作流：`.github/workflows/deploy-pages.yml`。
4. 在 `Settings → Pages → Build and deployment → Source` 选择 `GitHub Actions`。
5. 提交一个全新的 Commit。不要重跑旧任务。
6. 部署成功后打开 `VERSION.txt`，应显示 `V2.0.6 CACHE98`。

## APP

1. APP项目在 `android/`。
2. GitHub仓库中保留 `.github/workflows/release-apk.yml`。
3. 继续使用以前同一把正式签名密钥和四个 Actions Secrets。
4. 在 Actions 手动运行 `Build and publish Android APK`，标签留空，默认发布 `v2.0.6-cache98`。
5. 用户安装该Release中的 `jiuxiao-wendao-release.apk`。
6. 后续APP发现更高versionCode时才弹窗询问是否更新；没有新版本时不显示任何更新入口。

本版本没有新增SQL。
