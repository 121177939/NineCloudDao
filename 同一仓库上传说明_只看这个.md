# 只使用当前仓库，不新建仓库

本包同时包含网页和Android项目。解压后，将最外层全部内容覆盖到你现在的GitHub仓库main根目录。

## 网页

- 发布方式仍是原来的GitHub Actions：build → artifact → deploy。
- Settings → Pages → Source应为GitHub Actions；工作流会自动校正。
- Settings → Environments → github-pages中不得有等待时间、Required reviewers或自定义保护规则。
- 取消所有仍在运行的旧Pages工作流，再提交一次新commit。

## APP

- Android Studio项目在`android/`。
- APP没有右下角小圆圈。
- 自动检查同一仓库的GitHub Releases；发现更高versionCode时弹窗询问是否更新。
- APK不放进普通目录，必须由Actions中的`Build and publish Android APK`发布到Releases。
- 第一次发布前，在Settings → Secrets and variables → Actions中配置四个永久签名Secret：ANDROID_KEYSTORE_BASE64、ANDROID_KEY_ALIAS、ANDROID_KEYSTORE_PASSWORD、ANDROID_KEY_PASSWORD。
- 以后所有版本必须使用同一签名密钥。

本次没有新增SQL，也没有改变游戏数据库版本。
