# 九霄问道 V2.2.0 CACHE136 · SQL264 R2 / ADMIN9 R40

## 本次修复

SQL264 R1 的预检错误地要求生产修炼结算源码必须直接包含 `get_heaven_balance_v1`，因此在 SQL263 ONLINE 基线上会触发 `SQL264_PRECHECK_HEAVEN_BALANCE_NOT_IN_SETTLEMENT_CHAIN` 并整事务回滚。R2 已删除这条错误门禁，不再依赖函数源码字符串判断。

## 天道状态

ADMIN9 R40 可设置：自动动态、大道均衡、天道福泽、天道阻滞。R2 保留 SQL263 原始动态天道函数为私有备份，并以同名 `get_heaven_balance_v1()` 兼容包装器输出 GM 固定状态。

## NPC 正式道侣 ×1.5

正式天道人物 NPC 道侣 `status='active'` 存续期间，权威 `claim_cultivation_v1()` 最终修炼速度独立乘 `×1.5`；关系 ended 或删除后自动恢复 `×1.0`。这是最终乘法，不与普通 `character_cultivation_effects.multiplier_bonus` 加法池混算。

客户端修炼详情会显示 `NPC道侣同修 ×1.50（+50%）`。SQL264 R2 还会给 SQL 执行前已经存在的 active 正式道侣补齐状态标记。

## 部署顺序

1. 当前生产基线保持 SQL263 ONLINE。
2. 执行 `SQL264_R2_...执行即门禁.sql`。
3. 只有看到 `SQL264_GATE_PASSED` 才发布 CACHE136 R2 客户端。
4. 使用 ADMIN9 R40。
5. Edge `tiandao-ai CACHE135 R6` 不变，无需重新部署。

> SQL264 R1 执行失败发生在事务预检阶段，已自动回滚，不需要额外回滚脚本。不要再执行 R1。
