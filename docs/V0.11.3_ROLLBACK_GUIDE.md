# V0.11.3回退说明

V0.11.3没有数据库变更，因此不需要回滚SQL。

需要回退时：

1. 在GitHub仓库恢复V0.11.2 FIX1对应提交，或用V0.11.2 FIX1上传包覆盖；
2. Commit并Push；
3. 等待GitHub Actions完成；
4. 清理浏览器/PWA缓存。

注意：V0.11.2本身存在 `updateProgressionDisplay is not defined` 的运行时错误，回退仅用于紧急对比，不建议作为长期线上版本。数据库中的V0.11.2机缘数据、功法与玩家记录无需删除。
