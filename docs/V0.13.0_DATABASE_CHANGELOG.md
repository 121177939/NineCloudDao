# V0.13.0数据库变化

新增表：`progression_v0130_settings`、`cultivation_cap_adjustment_logs`。

新增字段：`character_breakthrough_states.total_failure_count`、`heavenly_insight_count`。

新增内部函数：修为上限、圆满判定、受控修为授予、触发器函数。

替换函数：突破状态、突破执行、自动修炼、机缘效果、赌场扣款/授予/开奖/市坊读取。

新增触发器：`trg_player_characters_cultivation_cap_v0130`。
