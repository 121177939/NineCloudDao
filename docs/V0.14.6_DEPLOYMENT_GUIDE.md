# V0.14.6部署指南

1. 备份Supabase数据库。
2. 执行V0.14.6正式SQL。
3. 确认末尾`effect_source_constraint`、`batch_fk_deferrable`、`opportunity_history_rpc`、`authenticated_execute`、`cache_epoch`全部为`true`。
4. 覆盖上传源码并等待Pages工作流通过。
5. 完全关闭旧页面后重新进入，确认V0.14.6 CACHE10。
6. 离线至少5分钟后重新登录，检查命书和修炼—机缘具体内容。
