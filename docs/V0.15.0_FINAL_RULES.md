# V0.15.0 最终规则

- 每次点击法印，按当前“数量 × 倍率”增加一次下注。
- 快速连续点击同一法印可合并为服务端一笔金额；赔率和结算按总下注金额线性计算，因此输赢结果不变。
- 所有服务端请求全局串行，避免并发响应覆盖新状态。
- 请求进行中按钮保持可点；封盘后才禁止继续下注。
- 下注离开页面后仍由服务端结算。
- 鱼虾灵局的个人下注和结算不进入九霄界闻。
- 灵骰问道、气运龟卜、造化池和其他既有界闻规则不变。
