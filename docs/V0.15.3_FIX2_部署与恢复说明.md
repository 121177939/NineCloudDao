# V0.15.3 FIX2部署与恢复说明

## 部署前

确认上传的是完整包而不是旧升级包；确认 `.github/workflows/deploy-pages.yml` 存在；确认Pages来源为GitHub Actions。

## 数据库路径

- 从V0.15.1全新升级：01→10顺序执行，每个检查脚本全部通过后再继续。
- 已执行07但出现 `record c has no field spirit_stones`：执行09、10。
- 已执行09成功：只执行10。

## 客户端

CACHE22会强制刷新旧PWA缓存。部署完成后若手机仍显示旧界面，应关闭所有标签页后重新打开，必要时清除该站点缓存。

## 禁止做法

不要向 `player_characters` 添加临时 `spirit_stones` 字段来绕过错误；不要恢复主修/辅修旧约束；不要再次运行已废弃的03_R1、03_R2或旧04检查脚本。
