# B V0.4 → A 正式合并审查

结论：B 的页面方向与 A 合并说明可用，但原包属于前端模块/接口合约，不具备独立生产上线条件。A 已补齐 SQL259、50 NPC、关系/记忆/缘遇/仙缘/道侣服务端、ADMIN9 R35 与正式版本门禁。

主要修正：人物筛选、多动作缘遇、Observer 范围、旧红尘客户端依赖、服务端权限、记忆增长边界、管理审计与玩家错误提示。

安全边界：玩家无权直接 SELECT/UPDATE 天道原始表；只能调用六个玩家 RPC。旧红尘三 RPC 撤销玩家执行权。Cloudflare/Supabase Secret 不进入客户端。
