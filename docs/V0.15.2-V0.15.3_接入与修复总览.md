# V0.15.2—V0.15.3接入与修复总览

## V0.15.2 CACHE19

接入B01命格方案A和造化池规则；A线重构功法系统并加入突破失败历史小境界保护。初版SQL先后修复了功法品级函数返回列类型不匹配、旧槽位重复数据触发唯一索引、检查脚本CTE引用缺失等问题。

## V0.15.3 CACHE20

修复GitHub Pages工作流仍调用旧V0.15.1固定CI导致构建失败的问题，工作流统一使用 `ci_v0153.py`。

## V0.15.3 FIX1 CACHE21

修复旧 `character_techniques_equipped_slot_check` 不接受 `ordinary_1`—`ordinary_5`，并移除主修、辅修、熟练度、传承点旧界面。

## V0.15.3 FIX2 CACHE22

修复功法RPC访问不存在的 `player_characters.spirit_stones`。普通功法读取、升级扣款和道卷兑换全部统一接入 `character_inventory` 灵石账户。
