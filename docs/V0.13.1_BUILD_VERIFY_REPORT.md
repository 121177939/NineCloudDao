# V0.13.1构建验证报告

生成日期：2026-07-26

## 结论

V0.13.1在干净源码目录、V0.12.0 FIX1覆盖升级目录、V0.13.0覆盖升级目录三种环境中均完成验证。所有当前有效检查均通过。

## V0.13.0失败复现

已使用真实V0.12.0 FIX1包模拟GitHub Desktop覆盖升级，再覆盖V0.13.0包。旧版验证器稳定返回：

```text
FAIL no-active-old-audit
TOTAL=53 PASS=52 FAIL=1
```

根因是旧文件`tools/audit_v0120_fix1.py`仍被Git跟踪和保留，而V0.13.0验证器要求它必须不存在。Node.js 20弃用信息属于warning，不是本次exit code 1的来源。

## V0.13.1验证

- V0.13.1发布元数据验证：通过。
- JavaScript语法检查：app.js、config.js、sw.js和市坊模拟脚本全部通过。
- V0.13.0继承SQL静态审计：23项通过、0项失败。
- 市坊页面模拟：12项通过。
- 最小Pages目录构建：通过。
- Pages制品独立验证：44项通过、0项失败。
- 本地HTTP读取：6项资源全部返回200。
- V0.12.0 FIX1覆盖升级模拟：通过。
- V0.13.0覆盖升级模拟：通过。
- GitHub工作流静态契约：全部通过。

## 工作流变化

- `actions/checkout@v6`
- `actions/setup-python@v6`，Python固定为3.13.5
- `actions/setup-node@v6`，Node固定为24
- `actions/configure-pages@v5`
- `actions/upload-pages-artifact@v4`
- `actions/deploy-pages@v4`
- build与deploy拆成两个job
- 只上传`.pages-site`最小白名单制品

## 数据库

V0.13.1无数据库变化，不需要新SQL。V0.13.0迁移已成功执行时不得重跑；尚未执行时仍按V0.13.0原部署说明执行。

## 未完成的外部步骤

本地无法替代用户真实GitHub Push、Hosted Runner执行和线上PWA真机验证。最终线上状态以新的GitHub Actions运行结果为准。
