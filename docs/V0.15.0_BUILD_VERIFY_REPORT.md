# V0.15.0 本地构建验证报告

本报告由 `tools/ci_v0150.py` 生成前的静态门禁定义支撑，最终结果记录在根目录 `V0.15.0_TEST_RESULTS.txt`。

验证范围：

- 版本、CACHE16和发布元数据；
- JavaScript语法；
- V0.14.1至V0.14.9继承测试；
- 鱼虾连续点击队列、合并金额、待提交提示与不锁按钮；
- V0.15.0 SQL静态审计；
- 九霄界闻 fish_shrimp 显式排除；
- GitHub Pages产物、文件清单和SHA256。

本地验证不能替代生产Supabase执行、真实GitHub Actions运行和多人并发真机测试。
