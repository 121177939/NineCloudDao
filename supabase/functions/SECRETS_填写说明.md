# Supabase Edge Functions → Secrets

请在 Supabase Dashboard 中新增：

- `CLOUDFLARE_ACCOUNT_ID`
- `CLOUDFLARE_AUTH_TOKEN`

本项目 Cloudflare Account ID：`5b79ea0b023989a461bbb8f8a6f0b374`

`CLOUDFLARE_AUTH_TOKEN` 请只在 Supabase 后台填写。**不要**写入本资料包、GitHub、Pages、Android assets、`config.js` 或 `app.js`。

配置 Secret 后再部署 `supabase/functions/tiandao-ai`，然后使用 ADMIN9 R36 → 天道人物 → “测试AI连接”验收。
