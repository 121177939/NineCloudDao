# 九霄问道 V2.2.0 CACHE128 · 天道人物 / 仙缘正式升级

## 发布基线
- 游戏：V2.2.0 CACHE128
- Android：versionCode 2001507 / versionName 2.2.0-cache128
- GM：ADMIN9 R35
- 当前生产数据库：SQL258 ONLINE
- 本版数据库：SQL259（执行成功必须返回 `SQL259_GATE_PASSED`）
- 下一 SQL：260

## B V0.4 正式合并结论
本版按 `docs/A_合并说明.md` 正式接管 B 模块，并由 A 补齐生产实现。

### 已彻底替换旧红尘玩家端
- 保留底部 `social` 导航位置与 `npcSocialSection` 容器。
- 玩家页面改为：总览 / 缘遇 / 人物志 / 仙缘 / 道侣。
- 客户端不再调用 `get_npc_social_v1`、`interact_with_npc_v1`、`form_npc_relationship_v1`。
- SQL259 撤销 anon/authenticated 对旧三 RPC 的执行权限；旧表与历史数据暂存归档，不直接 DROP。

### 50 名 NPC
- 核心人物 20 名。
- 可结缘人物 30 名：女20 / 男10。
- 全部 NPC 均为成年人。
- 30 名可结缘人物不限制玩家角色性别。

### 新关系模型
由独立天道关系驱动：好感、恩情、仇恨、恐惧、信任、亲密、情缘。
玩家端只展示其应知的公开关系表现与公开往事；精确负面关系、人物私有目标和内部决策备注仅 ADMIN9 R35 可查看。

### 缘遇与人物记忆
- 首次进入人物页会建立基础“听闻”关系，并按服务器规则生成缘遇。
- 缘遇支持多选项，不再是单按钮壳。
- 普通低重要记忆按人物关系最多保留最近50条，重要度4/5的关键记忆不参与该普通裁剪。
- 人物重大结缘继续写入已有九霄界闻，不创建第二套界闻。

### 仙缘 / 道侣
流程：真实互动 → 记忆 → 关系变化 → 达到表白门槛 → 服务端人物性格决策 accept / defer / reject → 正式道侣。
新版不以旧 `relationship_type=partner` 作为核心关系模型。

### AI 安全边界
B 包只提供了 Cloudflare AI 的安全要求与草案，没有可直接部署的生产 Edge Function。因此 CACHE128 的正式核心采用服务端 `server_personality_v1` 人格决策，能够完整运行且决策留审计。
Cloudflare AI 可作为后续服务端增强，但 `CLOUDFLARE_AUTH_TOKEN`、Supabase Secret/service role 永远不得进入网页或 APK。

## A 合并时修复的 B 模块问题
1. B 包无生产 SQL / 50 NPC 数据 / 服务端 RPC：由 SQL259 补齐。
2. 人物志筛选按钮原本无逻辑：已实现 全部/故交/知己/敌对。
3. 缘遇原本只渲染单一动作：已支持服务端 `actions[]` 多选项。
4. MutationObserver 原监听整个 document：改为只监听游戏 `#app`。
5. B 内旧 V0.3/CACHE124 标记：统一为正式 CACHE128。
6. 原游戏登录/同步/社交页仍依赖旧红尘 RPC：已从玩家运行时移除。
7. 玩家端错误提示由原始 RPC 文本改为可读中文提示。

## 上线顺序
1. 先执行 SQL259，确认最后结果是 `SQL259_GATE_PASSED`。
2. 再发布 CACHE128 Pages / Android。
3. 使用 ADMIN9 R35 → 天道人物 → “检查正式接入”，应返回 PASS。
4. 登录普通玩家，检查：人物总览、缘遇多选项、人物详情、交谈/赠礼/相约、仙缘门槛、道侣页。
