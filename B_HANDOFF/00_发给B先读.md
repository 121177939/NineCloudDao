# 发给B先读｜九霄问道 V1.7.2 CACHE48

本GitHub部署包同时作为B线下一轮开发的**唯一正式源码基线**。

## 当前锁

1. 正式版本：`V1.7.2 CACHE48`。
2. 开发锁：`V1.7.2_AB33_CACHE48`。
3. 下一补丁：`1.7.3`；下一正式SQL：`104`。
4. 已正式接入：`B-EQUIPMENT01`。
5. B线只能提交候选模块，不得自行修改正式版本、CACHE、开发锁或历史SQL。

## 本次装备界面修复

- 背包容量和界面统一为36格、6×6；
- 洞府只保留主游戏原生单一6×6格；
- 洞府装备嵌入原生空格，溢出时进入装备匣；
- 禁止重新增加第二套洞府格。

`B_HANDOFF/`不会进入Pages正式产物，同一ZIP可覆盖GitHub仓库，也可直接作为下一轮B开发基线。
