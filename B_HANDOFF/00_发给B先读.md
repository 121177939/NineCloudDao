# 发给B先读｜九霄问道 V1.7.6 CACHE55

本GitHub部署包是B线下一轮开发的唯一正式源码基线。

## 当前锁

1. 正式版本：`V1.7.6 CACHE55`。
2. 开发锁：`V1.7.6_AB40_CACHE55`。
3. 下一正式版本：`1.7.7`；下一正式SQL：`110`。
4. 最近正式接入B模块：`B-PAIGOW02-UI01`。
5. B线只能提交候选模块，不得自行修改正式版本、CACHE、开发锁或历史SQL。

## B-PAIGOW02-UI01 已正式并线

- `b-paigow02-ui.css/js` 已成为九霄灵牌正式视觉层。
- B02不得接管牌九RPC、发牌、私牌、Realtime、结算或资金权威。
- A线在B02并线后仅对 `paigow-app.js` 做多人性能调整；牌九玩法规则未改变。

## 多人公共桌性能硬锁

- 灵骰下注成功只做局部DOM补丁，不得恢复整桌 `replaceWith`。
- 灵骰阶段边界只做轻量增量同步；最近20局不得每个边界重复N+1读取。
- 公共轮次维护使用非阻塞单维护者；不得让所有客户端排队执行相同阶段推进。
- 牌九Realtime突发事件必须合并重绘；阶段到期客户端兜底必须使用 `advance_paigow_round_v176`。
- 禁止1秒数据库轮询、全局fetch劫持、长期MutationObserver扫整页。

## 装备界面锁继续有效

- 随身装备背包固定36格6×6并常驻元神页，无打开/关闭入口。
- 洞府只保留主游戏原生单一6×6储物格。

`B_HANDOFF/`必须随GitHub源码包交付，但不得进入Pages正式产物。
